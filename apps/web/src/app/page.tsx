'use client';

// AuthProvider takes care of redirecting away from `/`. This component
// only renders during the brief moment before that redirect lands.
export default function HomePage() {
  return null;
}
